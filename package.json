{
  "name": "my-portfolio-npm",
  "version": "1.0.0",
  "scripts": {
     "start": "parcel src/index.html --dist-dir dist --open",
    "build": "rimraf dist && parcel build src/index.html --dist-dir dist",
    "clean": "rimraf dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@parcel/transformer-sass": "^2.15.4",
    "parcel": "^2.15.4",
    "rimraf": "^6.0.1",
    "sass": "^1.77.6"
  }
}
